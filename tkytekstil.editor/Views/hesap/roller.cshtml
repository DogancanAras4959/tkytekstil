@model IEnumerable<tkytekstil.ENGINE.Dtos.RoleData.RoleDto>

@{
    ViewData["Title"] = "roller";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table mb-0">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Rol Adı</th>
                                <th>Oluşturulma</th>
                                <th>Durum</th>
                                <th>İşlem</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th scope="row">@item.ID</th>
                                    <td>@item.roleName</td>
                                    <td>@item.CreatedTime.ToString("MM/dd/yyyy HH:mm")</td>
                                    @if (item.IsActive == true)
                                    {
                                        <td>Aktif</td>
                                    }
                                    else
                                    {
                                       <td>Pasif</td>
                                    }
                                <td>
                                    <a class="btn btn-outline-secondary btn-sm edit" title="Düzenle" href="@Url.Action("rolduzenle","hesap", new { Id = item.ID })">
                                        <i class="fas fa-pencil-alt" title="Edit"></i>
                                    </a>
                                </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>
</div>
